import { Container } from "semantic-ui-react";

const { default: Root } = require("./navigation/Root");

function App() {
  return (
    <Container>
      <Root />
    </Container>
  );
}

export default App;
